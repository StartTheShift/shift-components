input(
  type = "text"
  ng-blur = "hide($event)"
  ng-class = "{'select-menu-visible': show_select_menu && options.length}"
  ng-focus = "onFocus($event)"
  ng-model="query"
  placeholder = "{{placeholder}}"
)
