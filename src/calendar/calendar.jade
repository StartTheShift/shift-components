.calendar
  table.table-condensed
    thead
      tr
        th(ng-click="goToPreviousMonth()")
          i.fa.fa-arrow-left

        th.month(colspan='5') {{ date.format('MMM Do, YY') }}

        th(ng-click="goToNextMonth()")
          i.fa.fa-arrow-right

      tr
        th Su
        th Mo
        th Tu
        th We
        th Th
        th Fr
        th Sa

    tbody(ng-click="selectDate($event)")
      tr(ng-repeat="week in weeks track by $index")
        td.available(
          ng-repeat = "day in week track by $index"
          ng-class = "{active: day.is_active, off: day.is_off }"
          data-iso = "{{ day.iso_8061 }}"
        ) {{ day.day_of_the_month }}
